#include <iostream>
#include <math>
#include <vector>
#define D double

using namespace std;

int search_JuicyWorms(){

}

int main(){
	ios::sync_with_stdio(0); 
	cin.tie(0);

	int N,M;
	while(cin >> N){					/*Se leen los n montones*/
		vector<int> W(N,0);				
		for(int i = 0; i < N; i++)		
			cin >> A[i];				/*Se leen los Ai-ésimos gusanos.*/
				
		cin >> M;						/*Se lee el #m de gusanos jugosos*/
		vector<int> Q(M,0);				
		for(int j = 0; i < M; j++)
			cin >> Q[j];				/*Se lee el j-ésimo gusano jugoso*/

	}
}